# Banco relacional + vetorial
DATABASE_URL=postgresql://bnbo:bnbo@localhost:5432/bnbo

# Cache / fila de jobs assíncronos (Celery)
REDIS_URL=redis://localhost:6379/0

# Modelo de embedding (multilingual-e5-large, 768d)
EMBED_MODEL_NAME=intfloat/multilingual-e5-large

# Transporte do servidor MCP: stdio | sse
MCP_TRANSPORT=stdio
MCP_HOST=127.0.0.1
MCP_PORT=8080

# Cliente de referência LangGraph (opcional, client_reference/)
MCP_SERVER_URL=http://127.0.0.1:8080/sse
MCP_CLIENT_MODEL=anthropic:claude-sonnet-4-6
# ANTHROPIC_API_KEY=...

